python-dotenv>=1.0.0
fastapi==0.115.0
uvicorn[standard]==0.30.6
beanie==1.26.0
motor==3.6.0
pydantic==2.9.2
python-multipart==0.0.12
aiofiles==24.1.0
openai>=1.57.0

# NLP — transformer models (Layer 1)
transformers>=4.40.0
torch>=2.2.0
# CPU-only torch (smaller download, ~500MB vs ~2GB):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# Local Whisper dependencies (optional - only needed if WHISPER_BACKEND=local)
# Uncomment these if you want to use local Whisper:
# git+https://github.com/openai/whisper.git
# torchaudio>=2.0.0
